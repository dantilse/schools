import {Link,useParams} from "react-router-dom"; import {getCampus,getPerson,getVerticalTeam,getVerticalTeamMembers} from "../lib/data";
export default function VerticalTeamDetail(){const {id}=useParams();const t=getVerticalTeam(Number(id));if(!t)return <div className="page"><h1>Vertical Team not found</h1></div>;const ms=getVerticalTeamMembers(t.vertical_team_id);return <div className="page"><div className="page-header"><div><p className="eyebrow">{t.school_year}</p><h1>{t.name} Vertical Team</h1><p className="muted">School leaders listed in the ACPTA assignment.</p></div></div><div className="detail-section"><h2>Members</h2><div className="table-wrap"><table><thead><tr><th>School</th><th>Person</th><th>Role</th><th>Designation</th></tr></thead><tbody>{ms.map(m=><tr key={m.vertical_team_member_id}><td>{m.campus_id?<Link to={`/schools/${m.campus_id}`}>{getCampus(m.campus_id)?.name}</Link>:"—"}</td><td>{getPerson(m.person_id)?.display_name}</td><td>{m.role_type}</td><td>{m.is_vertical_team_lead?"VT Lead":""}{m.is_coordinator?(m.is_vertical_team_lead?" · ":"")+"Coordinator":""}</td></tr>)}</tbody></table></div></div></div>}
