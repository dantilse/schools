import {Link} from "react-router-dom"; import {getCampus,getPerson,getVerticalTeamMembers,getVerticalTeams} from "../lib/data";
export default function VerticalTeams(){return <div className="page"><div className="page-header"><div><p className="eyebrow">Austin ISD</p><h1>Vertical Teams</h1><p className="muted">2025–26 assignments from Austin Council PTA.</p></div></div><div className="card-grid">{getVerticalTeams().map(t=>{const ms=getVerticalTeamMembers(t.vertical_team_id),hs=t.high_school_campus_id?getCampus(t.high_school_campus_id):undefined,lead=ms.find(m=>m.is_vertical_team_lead);return <Link className="card link-card" to={`/vertical-teams/${t.vertical_team_id}`} key={t.vertical_team_id}><div className="card-topline">{t.school_year}</div><h2>{t.name}</h2>{hs&&<p>{hs.name}</p>}<p className="muted">{ms.length} listed members</p>{lead&&<p><strong>VT Lead:</strong> {getPerson(lead.person_id)?.display_name}</p>}</Link>})}</div></div>}
